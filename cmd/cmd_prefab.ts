
const _fse: typeof import("fs-extra") = require("fs-extra");
const _path: typeof import("path") = require("path");

/** 定义名字 */
const defindName = {
    /** 命名规则 */
    role: {
        node_: "Node",
        label_: "Label",
        img_: "Sprite",

    }
}

/**
 * 从预制体生成代码模板，
 */
class cmd_prefab {

}
new cmd_prefab()
